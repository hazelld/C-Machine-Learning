language: c
before_install:
    - sudo apt-get install --only-upgrade -y cmake
install: 
    - ./configure
script: 
    - make 
    - cd ../
    - ./build/bin/sin_test
    - ./build/bin/xor_test
compiler:
    - clang
    - gcc

